# minimicro-projects

A handful of Mini Micro projects that I've done. To use these in Mini Micro, just mount the folder as `/usr2` and follow the instructions in the README (`cd "/usr2"; run "startup"` is probably good enough for most of these).
